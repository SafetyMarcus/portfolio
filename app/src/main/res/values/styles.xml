<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primaryColour</item>
        <item name="colorPrimaryDark">@color/primaryColourDark</item>
        <item name="colorOnPrimary">@color/textOnPrimary</item>
        <item name="colorAccent">@color/accentColour</item>
        <item name="colorOnSecondary">@color/textOnAccent</item>
        <item name="colorSurface">@color/surfaceColour</item>
    </style>

    <style name="AppTheme.Fullscreen" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentTransitions">true</item>
    </style>

    <style name="Splash" parent="AppTheme">
        <item name="android:windowBackground">@color/primaryColour</item>
    </style>

    <style name="H1" parent="TextAppearance.MaterialComponents.Headline4"/>

</resources>
